<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Games: Games Inventory</title>
    <link rel="stylesheet" href="/css/global.css" />
    <link rel="stylesheet" href="/css/games/index.css" />
  </head>
  <body>
    <%- include('../partials/header.ejs') %>

    <main>
      <h2><%= heading %></h2>

      <section>
        <a href="/games/add-game">+ Game</a>
      </section>

      <section class="games-container">
        <% games.forEach((game) => { %>
        <div>
          <article class="game-information">
            <p><%= game.name %></p>
            <p>Developer: <%= game.developer_names %></p>
            <p>Genre: <%= game.genre_names %></p>
            <p>Stock: <%= game.stock %></p>
          </article>

          <form action="/games/<%= game.id %>?_method=DELETE" method="POST">
            <button type="submit" class="btn-delete">Delete</button>
          </form>
        </div>
        <% }); %>
      </section>
    </main>

    <%- include('../partials/footer.ejs') %>
  </body>
</html>
